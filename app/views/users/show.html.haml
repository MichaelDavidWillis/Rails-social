.container.mb-4
  %h2
    This is #{@user.first_name}'s page

.container.mb-4
  = render "profile", user: @user

.container.mb-4
  = render "posts/form", user: @user

.container.mb-4
  %h3
    Posts:

  = render @user.posts.reverse unless @user.posts.empty?
